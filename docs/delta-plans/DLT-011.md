# Implementation Plan: DLT-011

**Delta Spec**: [../delta-specs/DLT-011.md](../delta-specs/DLT-011.md)
**Delta Design**: [../delta-designs/DLT-011.md](../delta-designs/DLT-011.md)

## Batch 1: Centralize badge clearing — ✓ Done
Depends on: — (none)

### Context & Research
- `docs/delta-designs/DLT-011.md` (S1, S2 shape parts, data flow diagrams) — defines where `mark_read` goes in `switch_to` and the reorder in `destroy_session`
- `docs/delta-specs/DLT-011.md` (R0, R1 acceptance criteria) — what must be verified after changes
- `src/session/manager.rs` — contains `switch_to` (line 788) and `destroy_session` (line 627), the two methods being modified
- `src/notifications/mod.rs` — `NotificationStore::mark_read` and `clear_session` contracts; `on_change` callback fires synchronously
- `docs/design/DES-002-callback-event-wiring.md` — `on_change` follows the callback wiring pattern; `mark_read` fires it synchronously within GTK main loop

### Steps

#### Step 1: Add `mark_read` to `switch_to`

- Modify: `src/session/manager.rs` — `switch_to` method (line 788)
- What to do: After `self.sidebar.set_active(session_id)` (line 793) and before `self.notify_state_changed()` (line 794), add:
  ```rust
  self.notification_store.borrow_mut().mark_read(session_id);
  ```
- Test: `cargo build` compiles. Existing tests pass (`cargo test`). Verify by code review: `mark_read` call appears after `set_active` and before `notify_state_changed`/`grab_focus`.

#### Step 2: Reorder `destroy_session` — move `clear_session` before `remove_tab`

- Modify: `src/session/manager.rs` — `destroy_session` method (line 627)
- What to do: Move `self.notification_store.borrow_mut().clear_session(session_id)` (currently at line 657) to just before `self.sidebar.remove_tab(session_id)` (currently at line 653). The new order should be:
  1. Clean up pane CWDs, browser state, bell timestamps
  2. Remove from GTK Stack
  3. `notification_store.clear_session(session_id)` — badge transitions to zero before row is removed
  4. `sidebar.remove_tab(session_id)` — row removed after badge is clean
  5. Remove split_views, sessions
  6. `notify_state_changed()`
  7. If was active → `switch_to(adjacent)`
- Test: `cargo build` compiles. Existing tests pass. Tab close with notifications no longer shows stale badge flash.

---

## Batch 2: Remove redundant `mark_read` callers — ⧗ Pending
Depends on: Batch 1

### Context & Research
- `docs/delta-designs/DLT-011.md` (S3, S4, S5 shape parts) — identifies exactly which call sites to remove
- `docs/delta-specs/DLT-011.md` (R2 acceptance criteria) — lists all call sites that must be removed and the one that must remain
- `src/app/keyboard.rs` — 6 `mark_read` sites (lines 189, 209, 219, 229, 253, 264) to remove; each follows a `switch_*` call that now triggers `mark_read` via `switch_to`
- `src/app/mod.rs` — `wire_tab_lifecycle` click handler (line 594) and collapsed-bar dot click handler (line 474) to remove
- `src/dropdown.rs` — `mark_read` in `show()` (line 189) must NOT be removed; it handles window visibility, not tab activation

### Steps

#### Step 3: Remove `mark_read` from keyboard handler — Alt/Ctrl PageUp/Down

- Modify: `src/app/keyboard.rs` — lines 188-190
- What to do: Remove the 3-line block:
  ```rust
  if let Some(active) = mgr.borrow().active_id() {
      notif_for_keys.borrow_mut().mark_read(active);
  }
  ```
  Keep the `mgr.borrow_mut().switch_adjacent(...)` call and `return glib::Propagation::Stop`.
- Test: `cargo build` compiles. Pressing Alt+PageDown/PageUp or Ctrl+PageDown/PageUp switches tabs and clears badge.

#### Step 4: Remove `mark_read` from keyboard handler — Alt+Shift PageUp/Down

- Modify: `src/app/keyboard.rs` — lines 208-210
- What to do: Remove the 3-line block (same pattern as Step 3) after `switch_adjacent_with_notifications` or `switch_adjacent` fallback.
- Test: `cargo build` compiles. Alt+Shift+PageDown/PageUp switches to notification tab and clears badge.

#### Step 5: Remove `mark_read` from keyboard handler — Ctrl+Alt PageUp/Down

- Modify: `src/app/keyboard.rs` — lines 218-220
- What to do: Remove the 3-line block after `switch_adjacent_group`.
- Test: `cargo build` compiles. Ctrl+Alt+PageDown/PageUp switches group and clears badge.

#### Step 6: Remove `mark_read` from keyboard handler — Ctrl+Shift PageUp/Down

- Modify: `src/app/keyboard.rs` — lines 228-230
- What to do: Remove the 3-line block after `switch_adjacent_running`.
- Test: `cargo build` compiles. Ctrl+Shift+PageDown/PageUp switches to running tab and clears badge.

#### Step 7: Remove `mark_read` from keyboard handler — Alt/Ctrl number keys

- Modify: `src/app/keyboard.rs` — lines 252-254
- What to do: Remove the 3-line block after `switch_to_visible_index`.
- Test: `cargo build` compiles. Alt+1 through Alt+9 switches to tab and clears badge.

#### Step 8: Remove `mark_read` from keyboard handler — Ctrl+Tab

- Modify: `src/app/keyboard.rs` — lines 263-265
- What to do: Remove the 3-line block after `switch_adjacent`.
- Test: `cargo build` compiles. Ctrl+Tab / Ctrl+Shift+Tab switches to adjacent tab and clears badge.

#### Step 9: Remove `mark_read` from wire_tab_lifecycle click handler

- Modify: `src/app/mod.rs` — lines 590-595
- What to do: In the `wire_tab_click` closure, remove `notif.borrow_mut().mark_read(&id)` (line 594). Also remove the now-unused `notif` clone (line 589). The closure becomes just `switch_to(&id)`.
- Test: `cargo build` compiles. Clicking a sidebar tab still switches to it and clears badge.

#### Step 10: Remove `mark_read` from collapsed-bar dot click handler

- Modify: `src/app/mod.rs` — lines 470-475
- What to do: In the `set_on_dot_click` closure, remove `notif_for_dot.borrow_mut().mark_read(&session_id)` (line 474). Also remove the now-unused `notif_for_dot` clone (line 471). The closure becomes just `switch_to(&session_id)`.
- Test: `cargo build` compiles. Clicking a dot in collapsed sidebar still switches to tab and clears badge.

#### Step 11: Clean up unused variables after removals

- Modify: `src/app/keyboard.rs`, `src/app/mod.rs`
- What to do: After removing all 6 `mark_read` calls, `notif_for_keys` is still used by `switch_adjacent_with_notifications` (line 200) — **do not remove it**. The `notification_store` parameter and `notif_for_keys` binding must stay. In `app/mod.rs`, remove the now-unused `notif` and `notif_for_dot` clones that were only used for `mark_read`. Run `cargo build` to identify any remaining unused-variable warnings.
- Test: `cargo build` compiles with no warnings about unused variables. `cargo clippy` passes.

#### Step 12: Verify no regressions

- What to do: Run `cargo test` — all existing notification-related tests must pass. Run `cargo clippy` — no new warnings. Verify the dropdown's `mark_read` in `show()` (`src/dropdown.rs:189`) is still present and unchanged. Verify active-session notification suppression: confirm `wire_pane_bell` in `src/session/manager.rs` still early-returns when the bell session is active (preventing badge increment for visible tabs). Verify `focus-session` socket command path: confirm `cmd_focus_session` calls `switch_to` which now triggers `mark_read` automatically.
- Test:
  - [ ] `cargo test` passes
  - [ ] `cargo clippy` passes with no new warnings
  - [ ] `src/dropdown.rs` line 189 still has `mark_read` call
  - [ ] No `mark_read` calls remain in `keyboard.rs` or `wire_tab_lifecycle`/dot-click handlers
  - [ ] `wire_pane_bell` still suppresses notifications for active sessions (code review)
  - [ ] `focus-session` command path reaches `switch_to` → `mark_read` (code review)

---

## Files Changed

- [x] `src/session/manager.rs` - Add `mark_read` to `switch_to`; reorder `clear_session` before `remove_tab` in `destroy_session`
- [x] `src/app/keyboard.rs` - Remove 6 redundant `mark_read` calls; `notification_store` binding stays (used by `switch_adjacent_with_notifications`)
- [x] `src/app/mod.rs` - Remove `mark_read` from click handler and dot click handler; clean up unused clones

## Notes

> **Batch status markers**: ⧗ Pending → ⧗ Implementing → ✓ Done
> Updated by the implementing agent as it progresses through batches.

> **Context & Research entries**: Each entry explains *why* it's needed. Mix of project docs
> (specs, designs, ADRs, DES), code files, and external research pointers (library docs, patterns
> to investigate). These are a starting point — the implementing agent adapts them as it goes
> (removing irrelevant ones, adding discovered files, updating references).

> **Code snippets in steps**: Use snippets to show *where* code goes and *what* it should do
> (via comments), not the complete implementation. The implementation agent has full access to
> spec, design, and decision documents — plans provide structural guidance, not code.

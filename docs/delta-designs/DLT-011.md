# Design: DLT-011 - Clear notification badge on tab activation and session cleanup

**Delta Spec**: [../delta-specs/DLT-011.md](../delta-specs/DLT-011.md)
**Status**: Draft

## Purpose

This document explains the design rationale for this delta: the modeling choices, data flow, system behavior, and architectural approach.

After implementation, the "Detected Impacts" section will guide reconciliation into feature design docs.

## Problem Context

[What specific problem does this delta solve? What constraints exist?]

## Design Overview

[High-level description of the design approach. What are the main components/concepts?]

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | Add `mark_read` to `switch_to` in `SessionManager` | |
| **S2** | Reorder `destroy_session` to call `clear_session` before `remove_tab` | |
| **S3** | Remove `mark_read` calls from `keyboard.rs` (6 sites: Alt/Ctrl PageUp/Down, Alt+Shift PageUp/Down, Ctrl+Alt PageUp/Down, Ctrl+Shift PageUp/Down, Alt/Ctrl number keys, Ctrl+Tab) | |
| **S4** | Remove `mark_read` from `wire_tab_lifecycle` click handler (`app/mod.rs`) | |
| **S5** | Remove `mark_read` from collapsed-bar dot click handler (`app/mod.rs`) | |

## Components

### Implementation Structure

| Layer/Component | Responsibility | Key Decisions |
|-----------------|----------------|---------------|

### Cross-Layer Contracts

**Integration Points**:

### Shared Logic

## Key Decisions

## System Behavior

## Open Questions

---

## Detected Impacts

### Affected Feature Designs

### Notes for Reconciliation

## Notes
